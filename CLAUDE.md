# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリ

静的パズルゲーム。ビルド工程も依存パッケージもない。`index.html` は構造だけを持ち、
見た目は `styles.css`、JS は `src/` の ES モジュールに分かれている。
テストは Node 標準のランナーだけで動き、push と PR で GitHub Actions が回す。

```sh
python3 -m http.server              # 手元で見る（file:// では動かない、後述）
npm test                            # 単体。依存も install も不要
npm run test:e2e                    # E2E（Chromium / Firefox / WebKit）
npm run test:all                    # 両方
node --test tests/rules.test.js     # 1ファイルだけ
```

**単体テストは依存ゼロで走る。** `"type": "module"` は Node が `src/*.js` を ES モジュールとして
読むために要るだけ。E2E だけが Playwright を必要とするので、単体を走らせるのに `npm install` は要らない。
CI も単体（Node 20/22/24）と E2E を別ジョブに分けてある。

## file:// では動かない

ES モジュールは `file://` オリジンが opaque 扱いになり CORS で拒否される（classic script は通る）。
そのため**ダブルクリックで開いても遊べない**。手元で確認するときは必ず簡易サーバを立てる。
この制約と引き換えにモジュール分割を選んでいるので、「file:// で開けるように」という理由で
classic script へ戻すなら、疎結合とテストの土台ごと設計し直すことになる。

## 層の分け方

| ファイル | 依存してよいもの |
|---|---|
| `rules.js` `palette.js` `tally.js` | 何にも依存しない |
| `puzzles.js` `game.js` | 上記のみ |
| `storage.js` `pwa.js` | 何にも依存しない（入れ物・登録先は注入される） |
| `view.js` | DOM と上記 |
| `main.js` | 全部 |

**`main.js` だけが配線を知っている。** 下の層から DOM・音・保存へ触らない。
この向きが崩れるとテストが書けなくなるので、新しい処理を足すときはまず
「どの層の話か」を決めてから書く。

外から来るものは注入する。`storage.js` は localStorage ではなく「入れ物」を、
`puzzles.js` は `Math.random` ではなく乱数関数を、`audio.js` は AudioContext の
コンストラクタを受け取る。おかげでブラウザなしで検証できている。

## 最短手数の算出（`rules.js`）

このゲームの心臓部。**単純なライツアウトではない**ので、素朴な実装に置き換えてはいけない。

- 1マスは3bit（`M=1` / `Y=2` / `B=4`）。`stamp()` が中心とその3×3に XOR を掛ける
- 色ごとに独立した GF(2) の連立一次方程式になる。3×3スタンプの**階数は16、核は9次元**
  （プラス型ライツアウトの2次元ではない）。1平面で到達できる盤面は 2^25 中 2^16 だけ
- **核の全要素の重みが偶数**なので、平面ごとに「解の手数のパリティ」が不変量になる。
  `minByParity()` が偶数側・奇数側それぞれの最小手数を返すのはこのため
- n手目に乗る色は `SEQ[moves % 3]` で固定され、**選べない**。だから最短手数は
  3平面の最小手数の和にならない。`trueMinimum()` は m を小さい順に試し、
  `slotCounts(m)` で三色へ配った枠に全平面が収まる最初の m を返す
- 到達できない盤面では `null` を返す。呼び出し側は「手数制限なし・表示は —」として扱う

`par` は入力上限も兼ねる（`game.js` が `moves >= par` を弾く）。だから揃った時点で
必ず最短手数で揃っている。

## 目に頼らない利用者への約束

この作品は色覚特性（P/D/T型）への配慮が土台にある。同じ基準を読み上げにも当てる。

- **配合は `aria-label` で伝える。** `paintCell()` が塗り直すたびに「3行2列 パープル」の形で
  書き換える。位置だけ名乗らせると、何が乗っているか分からなくなる
- **色以外の目印（帯・記号・数字）は飾りなので `aria-hidden="true"`。**
  これらは `display:none` ではなく `opacity:0` で見せ消ししており、外さないと
  読み上げツリーに残る。実際に 206 個の断片が露出していたことがある
- 色校正のマスは押せないが読めなければならないので `role="img"` を持つ。
  そのため **`data-i` は両方の盤に付いている**。セレクタは必ず `#board` か `#proof` で絞る

検証は Playwright の `getByRole` で名前を直接見る（`e2e/a11y.spec.js`）。
より厳密に見たいときは CDP の `Accessibility.getFullAXTree` でブラウザ本体のツリーを取る。

## 画面の狭さに関する約束

色を見比べる遊びなので、**目標と盤が同時に見えないと成立しない**。

- 600px 以下では色校正だけを縮める（`#proof{--cell:...}`）。見るだけなので小さくてよい
- **押す盤のマスは 44px を下回らせない。** 指で狙えなくなる
- 手応え（状態文）は盤の直下に置く。押した場所から離すと、変化に気づけない
- **横向きは支えないと決めた。** 幅が 667px まで広がると `max-width:600px` を抜けて色校正が縮まず、
  盤2枚が横に並びきらずに折り返して、目標と盤の距離が 800px になる（表示領域は 375px）。
  直すなら操作系を右へ寄せる二段組みが要る。決定は `manifest.webmanifest` の
  `orientation:portrait` と対になっていて、入れたあとの窓は縦で開く
- **指の端末では `:hover` が最後に触れた要素へ貼り付く。** 見た目を変える `:hover` は
  必ず `@media (hover:hover)` の中へ置く。盤に白枠が残ると色の比較が壊れる
- **マスに別の色を重ねさせない。** タップの既定の青枠（`rgba(51,181,229,.4)`）は
  `-webkit-tap-highlight-color` で消してある。消したぶんの手応えは `:active` で返す
- `e2e/layout.spec.js` が実機プロファイルで「同時に見えるか」を検査している。
  端末の再現は Chromium 専用なので、このファイルは chromium の project だけが拾う

## 触ると壊れるところ

### `palette.js` の `DIM` は CSS と結合している

```js
export const DIM = (1 - 0.20 / 3) * (1 - 0.26);
```

文字色は素の背景ではなく、ブラウン管表現のオーバーレイを通したあとの色で判定している。
`0.20/3` は `styles.css` の `.screen .grid::after`（3px周期に `rgba(0,0,0,.20)` が1px）、
`0.26` は `.screen .sheet::after` の落ち込み。**CSS 側を変えたら `DIM` も直す。**
放置すると文字が背景に溶ける。`tests/palette.test.js` が AA を検査しているので、
直し忘れればテストが落ちる。

### `PAL` の不変条件は「7色が等間隔」

一般色覚・P/D/T型の4通りで最小 ΔE が最大化されるよう数値最適化した組。
**知覚明度 L* が等間隔の階段になっているのはインクが乗った7色（1〜7）だけ**で、
白紙（0）はその階段より大きく下に離れている。「乗っているか否か」が一目で分かり、
配合の違いは等間隔で読める、という二層構造。1色だけ差し替えるとこの前提が崩れる。

### `sw.js` の一覧と `CACHE` の版は手で保つ

ビルド工程がないので、圏外用に取り込むファイルは `sw.js` に手で並べてある。

- **`cache.addAll` は1つでも取れなければ全部を捨てる。** 並べ間違えると Service Worker が
  一度も立たない。しかも画面は普通に動くので、圏外にしてみるまで気づけない
- `src/` にモジュールを足したら一覧にも足す。`tests/pwa.test.js` が
  `readdirSync("src")` と突き合わせて見張っている（実際に足し忘れた）
- **中身を差し替えたら `CACHE` の版を上げる。** 上げないと、すでに入れた人には古い一式が出続ける
- **`caches` はオリジン単位。** GitHub Pages では全プロジェクトが同じオリジンに乗るので、
  古い取り込みの掃除は `PREFIX` で絞る。絞らないとよそのプロジェクトのぶんまで消える

### CSP は書き忘れたものを静かに殺す

`default-src 'none'` は「書き忘れた全ディレクティブが `'none'`」を意味する。新しい仕組みを
足すときは、必要なディレクティブを `index.html` の `<meta>` に**明示的に**書く。

- マニフェストは `manifest-src` に落ちる。書かないとブロックされる
- Service Worker は `worker-src` に落ちる（`script-src` へのフォールバック任せにせず書いてある）
- **Chrome はマニフェストを遅延取得する。** ページを開いてコンソールを眺めるだけでは
  リクエストが飛ばず、遮断されていても静かなまま。`e2e/pwa.spec.js` が CDP の
  `Page.getAppManifest` で取得を強制しているのはこのため

### 保存値は信用しない

`storage.js` の `sanitize()` が唯一の入口。GitHub Pages のユーザーサイトは
**全プロジェクトが同一オリジン**（`m-masaki72.github.io`）なので、この値は
他のリポジトリのページからも書き換えられる。自分でしか壊せない値ではない。
項目を足すときは必ず値域か許可リストで検証し、`tests/storage.test.js` に
壊れた入力のケースを足す。`reached` の下限が抜けていて起動不能になった前例がある。

`store` が `null` になる環境（localStorage が例外を投げる）では、保存を静かに諦めて
UI にその旨を出しつつ遊びは続く。この分岐を消さない。

## テストの方針

`tests/` は純ロジックを `node --test` で、`e2e/` は実ブラウザでの振る舞いを Playwright で検査する。
**層の境目は「ブラウザが要るか」**。盤の規則や保存値の検証は単体へ、描画・入力・CSP・保存の往復は E2E へ置く。

意味のあるテストは「コードを読んでも分からない保証」を書いたもの。既存のものだと、
練習21面すべてで `par === 手数`（崩れると答えの手順を押し切る前に手数制限へ当たる）、
`FALLBACK` が本当に検算どおりか、生成した出題が必ず選んだ難易度ちょうどになるか、あたり。
ゲッターの動作確認のようなテストは足さない。

**テストを書く前に、主張しようとしている前提を実測する。** 「イエロー単独の盤面は2手以上」と
思い込んで書いたテストが落ち、実際は到達不能（`null`）だった、という例がある。

圏外の検証は `context.setOffline(true)` のあと `reload()` して、盤が組まれて実際に押せるかまで見る。
Service Worker とマニフェストの検査は CDP を使うので、`e2e/pwa.spec.js` も chromium 専用。

E2E では `addInitScript` で保存値を仕込まない。**再読み込みのたびに再実行されて保存値を
上書きしてしまい、「設定が保存されるか」を検証できなくなる。** `e2e/helpers.js` の `openGame()` は
一度読み込んでから `evaluate` で書き、`reload()` する。ここを踏んで二度はまっている。

出題はランダムなので、E2E は練習面（毎回同じ盤面）に固定して検証する。`STAGE` 定数がその入口。

## 書き方の約束

- ES2015+ の素の JS。フレームワークもトランスパイルもない
- UI文字列とコメントは日本語。印刷の比喩（色校正／刷り台／インク／校了／刷り上がり）で
  語彙が統一されているので、新しい文言もそこに合わせる
- コメントは「コードから読み取れない理由」だけ。この密度を保つ
- **動きを減らす設定でも手応えを消さない。** `prefers-reduced-motion` は動きを避けるためのもので、
  変化そのものを禁じてはいない。横揺れ（`nudge`）は止めるが、明滅（`blocked-flash`）に差し替える。
  `animation:none` で消すと、音を切っている人には何も残らない
- **右クリックを前提にした文言を書かない。** `matchMedia("(pointer: coarse)")` で出し分ける（`TEXT`）
- CSP を `index.html` の `<meta>` に置いている。`default-src 'none'` で外部送信を塞いでいるので、
  新しい読み込み先が要るときはここも開ける。`frame-ancestors` は meta では無視される
- **`style-src` から `'unsafe-inline'` を外してあるので、マークアップに `style="..."` を書いても効かない。**
  エラーは出ず静かに無視されるだけなので気づきにくい。見た目は必ず `styles.css` の規則で書く。
  JS からの `el.style.x = ...` は CSSOM 操作なので CSP の対象外で、これまでどおり動く
