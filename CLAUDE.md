# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリ

`index.html` 1ファイルで完結する静的パズルゲーム。**ビルド・依存・テスト・lint は存在しない**（`package.json` も CI もない）。HTML / CSS / JS はすべて `index.html` の中にあり、`<style>` → マークアップ → `<script>` の順に並ぶ。

## 動かす・確かめる

```sh
open index.html                 # file:// で完全に動く
python3 -m http.server 8000     # サーバ越しに見たいときだけ
```

自動テストがないため、検証はブラウザ操作で行う。最低限、練習1面目・難易度3つの出題・記録を消す、の3経路を触る。

## 設計の中核

### 盤面は独立した3枚の GF(2) 平面

- 1マスは3bit（`M=1` / `Y=2` / `B=4`）。`stamp()` が中心とその3×3に XOR を掛ける（盤外は切り落とし）
- `MASK[i]` は「マス i を押したときに立つ25bit」。起動時の IIFE がこれを掃き出して `piv`（ピボット）と `kernel`（核）を作る
- **核の次元は 9**（`1<<9 = 512` 通り）。プラス型のライツアウトではなく3×3スタンプなので階数は16しかなく、1平面あたり到達可能な盤面は 2^25 中 2^16 だけ。`solvePlane()` が `null` を返すのは像の外の盤面

### `trueMinimum()` がこのゲームの心臓部

単純なライツアウトと決定的に違うのは **色を選べない**点。n手目に乗るインクは `SEQ[moves%3]` で機械的に決まる。そのため最短手数は「3平面それぞれの最小手数の和」にはならない。

1. `minByParity(target)` — 核の512通りを全探索し、**偶数重みの最小・奇数重みの最小**を返す。同じマスを2度押すと重みが +2 されてパリティが変わらないので、パリティごとの最小さえ分かれば「n手枠に収まるか」を判定できる
2. `trueMinimum()` — `m = 0..40` を小さい順に試し、`slotCounts(m)` で3色への手数配分を出す。各色について「配分数以下 かつ 同パリティ」で解けるかを見て、3色すべて成立した最初の `m` が最短手数

この値が `par` として**入力上限も兼ねる**（`onCell()` が `moves >= par` を弾く）。だから揃った時点で必ず最短手数で揃っている、という設計。

### 出題は `applyPuzzle()` の一本道

- `newGame()` — ランダムな押下列を生成し `trueMinimum() === level` になるまで最大400回引き直す。見つからなければ検算済みの `FALLBACK` を使い、詰ませない
- `loadStage(n)` — `TUTORIAL` の21面。各面は**押すマス番号の配列 `s` だけ**を持ち、色は順番から自動で決まる。面を足す・直すときはマス番号を書くだけでよい
- `answer`（答えを見る機能）は生成列そのもの。生成時点で最短であることが保証されているので、別途解き直していない

## 触るときに壊しやすい結合

### CSS のオーバーレイと `contrastOn()` の `DIM`

```js
var DIM = (1 - 0.20/3) * (1 - 0.26);
```

文字色はマスの素の色ではなく、**ブラウン管表現のオーバーレイを通したあとの色**で判定している。`0.20/3` は `.screen .grid::after` の走査線（3px周期に `rgba(0,0,0,.20)` が1px）、`0.26` は `.screen .sheet::after` の曲面落ち込み分。**これらの CSS を変えたら `DIM` も直す。** 放置すると文字が背景に溶ける。

### `PAL` の8色

一般色覚・P型・D型・T型の4通りすべてで最小 ΔE が最大になるよう数値最適化した値で、かつ明度が等間隔の階段になるよう配置してある。色覚に依存しない判別はこの階段が土台なので、**1色だけ差し替えると前提が崩れる**。

## 状態の持ち方

- ゲーム状態はモジュールスコープの変数（`board` / `target` / `moves` / `par` / `history` / `stage` / `solved`）
- **トグルUIの真値は DOM の `aria-pressed`**。`draw()` は `elDiff.getAttribute("aria-pressed")` を読みに行く。`segWire()` が排他更新、`setSeg()` が保存からの復元を担う。新しいトグルもこの流儀に合わせる
- 保存は `localStorage["inktrio.v1"]`、ペイロードは `{v:2, ...}`（キー名の `v1` とペイロードの `v` は別物）。`load()` は v1 / v2 を受け付ける。項目を足すときは `save()` と `boot()` の両方を触り、`boot()` 側では必ず値域を検証してから採用する（既存は全項目でそうしている）
- `store` は `null` になりうる（localStorage が例外を投げる環境）。保存は静かに諦め、UI はその旨を出して動き続ける。**この分岐を消さない**
- 日付またぎは `rollDay()` が担当。今日ぶんだけ 0 に戻し、通算には触らない

## アクセシビリティの前提

- 色以外の目印を4通り用意している（帯 / 記号 M・Y・B / 数字 0〜7 / なし、`markMode`）。**色だけで情報を伝える表現を新たに足さない**
- キーボードだけで全機能に届く（矢印・Enter・Z / Backspace・Escape）
- `@media (prefers-reduced-motion:reduce)` でアニメーションを止めている。新しいアニメーションはここにも追記する

## 書き方の約束

- JS は ES5 スタイルで統一（`var`、`function`、IIFE、`Array.prototype.forEach.call`）。トランスパイルがないので既存に合わせる
- 音は Web Audio による合成のみで外部音源はない。`ac()` が初回呼び出しで AudioContext を作る（自動再生ポリシー対策の遅延生成）。全音源は master → DynamicsCompressor → destination を通す
- UI文字列とコメントは日本語。印刷の比喩（色校正／刷り台／インク／校了／刷り上がり）で語彙が統一されているので、新しい文言もそこに合わせる
- コメントは「コードから読み取れない理由」だけが書かれている。この密度を保つ

